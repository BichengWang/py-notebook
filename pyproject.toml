[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py-notebook"
version = "0.1.0"
description = "A Python notebook"
dependencies = [
    "yfinance",
    "numpy",
]
